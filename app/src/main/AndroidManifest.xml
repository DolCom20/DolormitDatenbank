<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dolortagebuch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Schmerzhinten"></activity>
        <activity android:name=".Schmerzlinks" />
        <activity android:name=".Schmerzrechts" />
        <activity android:name=".Schmerzvorne" />
        <activity android:name=".Kalender" />
        <activity android:name=".Registrieren2" />
        <activity android:name=".Registrieren" />
        <activity android:name=".Einloggen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>